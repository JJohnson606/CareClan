<%= form_with(model: approval) do |form| %>
  <% if approval.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(approval.errors.count, "error") %> prohibited this approval from being saved:</h2>

      <ul>
        <% approval.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :votetype, style: "display: block" %>
    <%= form.check_box :votetype %>
  </div>

  <div>
    <%= form.label :voter_id, style: "display: block" %>
    <%= form.text_field :voter_id %>
  </div>

  <div>
    <%= form.label :post_id, style: "display: block" %>
    <%= form.text_field :post_id %>
  </div>

  <div>
    <%= form.label :approvals_count, style: "display: block" %>
    <%= form.number_field :approvals_count %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
