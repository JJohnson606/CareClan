<% if policy(@clan).new? %>
  <div class="container mx-auto py-8">
    <h1 class="text-3xl font-bold mb-4">Create a New Clan</h1>
    <%= render 'form', clan: @clan, submit_label: 'Create Clan', cancel_path: clans_path %>
  </div>
<% end %>